'use strict';

var Raid = require('../models/raid');

var Actions = {
  processMessage: function (data) {
    switch(data.action) {
      case 'add':

        break;
      case 'move':

        break;
      case 'remove':

        break;
    }
  }
}

module.exports = Actions;
