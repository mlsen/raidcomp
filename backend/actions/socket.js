var Raid = require('../models/raid').model;

var Actions = {
  processMessage: function (data) {
    switch(data.action) {
      case 'add':

        break;
      case 'move':

        break;
      case 'remove':

        break;
    }
  }
}

module.exports = Actions;
